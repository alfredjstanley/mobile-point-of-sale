const districtDTO = require("./district.dto");
const stateDTO = require("./state.dto");
const taxDTO = require("./tax.dto");

module.exports = {
  DistrictDTO: districtDTO,
  StateDTO: stateDTO,
  TaxDTO: taxDTO,
};
