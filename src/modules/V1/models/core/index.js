const storeModel = require("./store.model");
const authUserModel = require("./authUser.model");
const userProfileModel = require("./userProfile.model");

module.exports = {
  Store: storeModel,
  AuthUser: authUserModel,
  UserProfile: userProfileModel,
};
